<input type="file" class="textbox" name="{{ inputName }}" size="{{ inputSize }}"

       value="{{ userValue }}" tabindex="{{ fieldTabIndex }}"
       id="{{ inputID }}" {{ patternElement }}

       step="{{ fieldData.step_size }}"
       form="contentForm" {{ acceptElement }} {{ captureElement }} {{ requiredElement }}/>
