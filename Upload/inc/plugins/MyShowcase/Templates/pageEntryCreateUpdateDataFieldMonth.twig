<input type="month" class="textbox" name="{{ inputName }}" size="{{ inputSize }}"

       value="{{ userValue }}" tabindex="{{ fieldTabIndex }}"
       id="{{ inputID }}" {{ patternElement }}
       min="{{ minimumValue }}" max="{{ maximumValue }}"
       step="{{ fieldData.step_size }}" form="contentForm" {{ requiredElement }}/>
